// Leia 2 números (A, B) e escreva-os em ordem inversa (B, A).
import {question} from 'readline-sync'

 // entrada
var n1 = Number(question('primeiro numero: '))
var n2 = Number(question('segundo numero: '))

 // processamento


 // saída
 console.log(`> Os numeros (${n1},${n2}) é o oposto de (${n2},${n1}).`)